<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lonn.studentassistant">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".authentication.AuthenticationActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="www"
                    android:host="lonn" />
            </intent-filter>

        </activity>

        <activity
            android:name=".student.StudentActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".authentication.AuthenticationActivity"/>

        <service android:name=".authentication.services.LoginService" />
        <service android:name=".authentication.services.RegisterService" />
        <service android:name=".authentication.services.CredentialsCheckService" />
        <service android:name=".globalServices.studentService.StudentService" />
        <service android:name=".globalServices.coursesService.CourseService" />
    </application>

</manifest>