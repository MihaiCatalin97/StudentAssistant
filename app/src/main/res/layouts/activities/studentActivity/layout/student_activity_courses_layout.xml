<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        style="@style/DarkBlueTitle"
        android:id="@+id/titleCoursesLayout"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Courses"
        app:layout_constraintTop_toTopOf="parent"/>

    <com.lonn.studentassistant.views.implementations.scrollViewLayouts.CoursesFullScrollView
        android:id="@+id/layoutCoursesCategories"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titleCoursesLayout"
        tools:context = ".activities.implementations.student.StudentActivity"/>


</android.support.constraint.ConstraintLayout>